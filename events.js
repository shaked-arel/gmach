const AddEventButton= document.getElementById("add-button")
AddEventButton.addEventListener("click", function(){
    console.log("clicked")
    addEvent()
})

function addEvent(){
    // add to firebase
    // go the a new window to fill information
    // go to choose the items list
}